'use client';
import Link from 'next/link'; import { useEffect, useState } from 'react'; import { ErrorState, LoadingState } from '../../../components/page-state'; import { api } from '../../../lib/api';
interface Overview { customers: number; transactions: number; held: number; assessments: number; }
export default function AdminOverviewPage() { const [data, setData] = useState<Overview | null>(null); const [error, setError] = useState(''); useEffect(() => { Promise.all([api.adminCustomers(), api.adminTransactions(), api.adminHeldTransactions(), api.adminAssessments()]).then(([customers, transactions, held, assessments]) => setData({ customers: customers.total, transactions: transactions.total, held: held.total, assessments: assessments.total })).catch((reason: Error) => setError(reason.message)); }, []); if (error) return <ErrorState message={error} />; if (!data) return <LoadingState />; return <section><h2 className="text-xl font-bold">Overview</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Customers', data.customers, '/admin/customers'], ['Transactions', data.transactions, '/admin/transactions'], ['Held reviews', data.held, '/admin/held'], ['Fraud assessments', data.assessments, '/admin/fraud']].map(([label, value, href]) => <Link key={String(label)} href={String(href)} className="rounded-2xl bg-white p-5 ring-1 ring-slate-200 hover:ring-blue-400"><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></Link>)}</div></section>; }
