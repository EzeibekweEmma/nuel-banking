'use client';
import { useEffect, useState } from 'react'; import { AdminTransactionList } from '../../../../components/admin-transaction-list'; import { EmptyState, ErrorState, LoadingState } from '../../../../components/page-state'; import { AdminTransaction, api } from '../../../../lib/api';
export default function TransactionsPage() { const [items, setItems] = useState<AdminTransaction[] | null>(null); const [error, setError] = useState(''); useEffect(() => { api.adminTransactions().then((result) => setItems(result.data)).catch((reason: Error) => setError(reason.message)); }, []); if (error) return <ErrorState message={error} />; if (!items) return <LoadingState />; return <section><h2 className="text-xl font-bold">Transactions</h2>{items.length ? <AdminTransactionList items={items} /> : <div className="mt-5"><EmptyState message="No transactions found." /></div>}</section>; }
