'use client';
import { useEffect, useState } from 'react'; import { EmptyState, ErrorState, LoadingState } from '../../../../components/page-state'; import { api, AuditLog } from '../../../../lib/api';
export default function AuditLogsPage() { const [items, setItems] = useState<AuditLog[] | null>(null); const [error, setError] = useState(''); useEffect(() => { api.adminAuditLogs().then((result) => setItems(result.data)).catch((reason: Error) => setError(reason.message)); }, []); if (error) return <ErrorState message={error} />; if (!items) return <LoadingState />; return <section><h2 className="text-xl font-bold">Audit logs</h2><div className="mt-5 overflow-hidden rounded-2xl bg-white ring-1 ring-slate-200">{items.map((item) => <div key={item.id} className="grid gap-1 border-b border-slate-100 p-4 text-sm last:border-0 sm:grid-cols-[1fr_1fr_auto]"><b>{item.action}</b><span className="text-slate-600">{item.user?.email ?? 'System'} · {item.entityType}</span><span className="text-slate-500">{new Date(item.createdAt).toLocaleString()}</span></div>)}{items.length === 0 && <div className="p-5"><EmptyState message="No audit logs found." /></div>}</div></section>; }
