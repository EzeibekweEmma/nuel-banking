import { Account } from '../lib/api';
export function BalanceCard({ account }: { account: Account }) { return <section className="rounded-2xl bg-gradient-to-br from-blue-700 to-indigo-900 p-6 text-white"><p className="text-sm text-blue-100">Available balance</p><p className="mt-2 text-4xl font-bold">{new Intl.NumberFormat('en-NG', { style: 'currency', currency: account.currency }).format(Number(account.balance))}</p><div className="mt-8 flex justify-between text-sm text-blue-100"><span>{account.type} account</span><span>•••• {account.accountNumber.slice(-4)}</span></div></section>; }
