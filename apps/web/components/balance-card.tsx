import { Account } from '../lib/api';
export function BalanceCard({ account }: { account: Account }) { return <section className="overflow-hidden rounded-3xl bg-gradient-to-br from-blue-700 via-blue-800 to-indigo-950 p-6 text-white shadow-lg shadow-blue-950/15 sm:p-8"><p className="text-sm font-medium text-blue-100">Available balance</p><p className="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">{new Intl.NumberFormat('en-NG', { style: 'currency', currency: account.currency }).format(Number(account.balance))}</p><div className="mt-10 flex flex-wrap justify-between gap-3 border-t border-white/15 pt-4 text-sm text-blue-100"><span className="uppercase tracking-wide">{account.type} account</span><span>•••• {account.accountNumber.slice(-4)}</span></div></section>; }
